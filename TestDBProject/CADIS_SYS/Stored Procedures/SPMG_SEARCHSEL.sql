CREATE PROCEDURE [CADIS_SYS].[SPMG_SEARCHSEL]
@SelectSQL VARCHAR (MAX), @FromSQL VARCHAR (MAX), @WhereSQL NVARCHAR (MAX), @KeyMergeSQL VARCHAR (MAX), @AliasedKeyMergeSQL VARCHAR (MAX), @StartValue VARCHAR (MAX), @Forward BIT, @RowsIn INT, @RowsOut INT OUTPUT, @FirstValue VARCHAR (MAX) OUTPUT, @LastValue VARCHAR (MAX) OUTPUT, @HasPrevious BIT OUTPUT, @HasNext BIT OUTPUT
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


